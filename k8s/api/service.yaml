apiVersion: v1
kind: Service
metadata:
  name: task-api
  namespace: task-management
spec:
  type: ClusterIP

  selector:
    app: task-api

  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP