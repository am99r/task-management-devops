apiVersion: v1
kind: Service
metadata:
  name: task-db
  namespace: task-management
spec:
  type: ClusterIP

  selector:
    app: task-db

  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP